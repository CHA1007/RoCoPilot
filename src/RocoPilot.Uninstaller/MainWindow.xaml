<ui:FluentWindow x:Class="RocoPilot.Uninstaller.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="卸载 RocoPilot"
                 Width="540" Height="520"
                 ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterScreen"
                 ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl x:Name="PageHost" Grid.Row="0" />

        <Border Grid.Row="1" Padding="24,12"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button x:Name="CancelButton" Grid.Column="0"
                        Content="取消" MinWidth="96"
                        Click="Cancel_Click" />
                <ui:Button x:Name="UninstallButton" Grid.Column="2"
                        Content="卸载" MinWidth="96" Appearance="Danger"
                        Click="Uninstall_Click"
                        Visibility="Collapsed" />
                <ui:Button x:Name="CloseButton" Grid.Column="2"
                        Content="关闭" MinWidth="96" Appearance="Primary"
                        Click="Close_Click"
                        Visibility="Collapsed" />
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
